from settings import *

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'denorm',
        'HOST': 'localhost',
        'USER': 'denorm',
        'PASSWORD': '',
    }
}
